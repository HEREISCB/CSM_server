# Prometheus Configuration for CSM TTS Server

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'csm-tts'
    static_configs:
      - targets: ['tts-server:8080']
    metrics_path: '/metrics'
    scrape_interval: 10s
    scrape_timeout: 5s
